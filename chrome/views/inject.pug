doctype html

html
  head
    meta(charset='UTF-8')
    title Redux TodoMVC Example (Inject)
    style.
      html, body {
        background-color: rgba(0,0,0,0)
      }
  body
    #root
    script(src=env == 'prod' ? '/js/popup.bundle.js' : 'https://localhost:4000/js/popup.bundle.js')
